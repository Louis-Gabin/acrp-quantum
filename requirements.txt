# Core (offline pipeline: instance, QUBO, brute force, simulated annealing, demo)
numpy>=1.26
matplotlib>=3.8
pandas>=2.0

# Classical optimiser used by the QAOA parameter search
scipy>=1.11

# Quantum campaign (QAOA on Aer). Pin to a recent Qiskit 1.x / 2.x release.
qiskit>=1.2
qiskit-aer>=0.15

# Optional: emulated quantum annealing baseline (D-Wave Ocean)
# dwave-neal>=0.6
# dwave-ocean-sdk>=7.0

# Classical exact baseline (David Rey): Gurobi >= 12 (academic licence), uses nlfunc
# gurobipy>=12.0
